print("Start import numpy", flush=True)
import numpy
print("numpy imported successfully", flush=True)
print("numpy version:", numpy.__version__, flush=True)

print("Start import sklearn", flush=True)
import sklearn
print("sklearn imported successfully", flush=True)
print("sklearn version:", sklearn.__version__, flush=True)
